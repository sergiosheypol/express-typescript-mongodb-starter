{
  "name": "express-typescript-mongo-starter",
  "version": "0.1.0",
  "description": "Express + Typescript + MongoDB boilerplate",
  "main": "server.ts",
  "scripts": {
    "clean": "npx tsc --build --clean",
    "build": "npx tsc",
    "test": "npm run build && npx mocha \"./dist/**/*.test.js\"",
    "start": "npm run build && node dist/server.js"
  },
  "author": "Sergio Sheypol",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.13.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongodb": "^5.0.1",
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.1"
  }
}
